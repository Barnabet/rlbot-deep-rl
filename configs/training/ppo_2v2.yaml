# 2v2 PPO training configuration

environment:
  team_size: 2
  spawn_opponents: true
  timeout_seconds: 300

training:
  n_workers: 24
  n_envs_per_worker: 4

ppo:
  batch_size: 150_000
  minibatch_size: 37_500

rewards:
  team_spirit: 0.3
  team_spacing_penalty: -0.1
