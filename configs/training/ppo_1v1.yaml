# 1v1 PPO training configuration

environment:
  team_size: 1
  spawn_opponents: true
  timeout_seconds: 300

training:
  n_workers: 16
  n_envs_per_worker: 4

ppo:
  batch_size: 50_000
  minibatch_size: 10_000
  n_epochs: 10

rewards:
  team_spirit: 0.0
  team_spacing_penalty: 0.0
